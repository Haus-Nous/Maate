{
  "name": "@maate/shared-types",
  "version": "0.1.0",
  "private": true,
  "description": "Shared TypeScript types and API contracts for Maate",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsup src/index.ts --format cjs,esm",
    "clean": "rm -rf dist .turbo",
    "lint": "eslint src/",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@maate/tsconfig": "workspace:*",
    "tsup": "^8.0.0",
    "typescript": "^5.6.0"
  }
}
